<!-- MOBILE NAVIGATION -->
    <nav class="mobile-nav">
        <div class="mobile-nav-container">
            <button class="mobile-nav-button" onclick="document.getElementById('Bouton_gauche').click()"> {# onclick.click sert à cliquer sur la balise <a> qui est plus petite#}
                {% block boutton_gauche %}{% endblock %}            
            </button>
            {% if user and user.id != 999 %}
            <button class="mobile-nav-button" onclick="document.getElementById('Bouton_milieu').click()"> {# onclick.click sert à cliquer sur la balise <a> qui est plus petite#}
                {% block boutton_milieu %}{% endblock %}
            </button>
            {% endif %}
            <button class="mobile-nav-button" onclick="document.getElementById('Bouton_droite').click()"> {# onclick.click sert à cliquer sur la balise <a> qui est plus petite#}
                {% block boutton_droite %}{% endblock %}
            </button>
        </div>
    </nav>